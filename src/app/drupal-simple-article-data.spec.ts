import { DrupalSimpleArticleData } from './drupal-simple-article-data';

describe('DrupalSimpleArticleData', () => {
  it('should create an instance', () => {
    expect(new DrupalSimpleArticleData()).toBeTruthy();
  });
});
