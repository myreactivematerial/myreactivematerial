import { DrupalSimpleArticle } from './drupal-simple-article';

describe('DrupalSimpleArticle', () => {
  it('should create an instance', () => {
    expect(new DrupalSimpleArticle()).toBeTruthy();
  });
});
